#include <iostream>

/*
int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	uint32_t N;
	std::cin >> N;

	long long min = LLONG_MAX;
	long long max = LLONG_MIN;
	for (long i = 0; i < N; ++i) {
		long long m;
		long long v;
		std::cin >> m >> v;

		long long t = m + v;
		if (t > max) max = t;
		if (t < min) min = t;
	}

	std::cout << max - min << '\n';
}
*/