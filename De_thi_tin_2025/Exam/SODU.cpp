#include <iostream>

/*
int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	uint32_t N;
	std::cin >> N;

	uint64_t first = 1;
	for (uint64_t i = 0; i < N; ++i) {
		first *= 3;
	}

	uint64_t res = 1;
	for (uint64_t i = 0; i < first; ++i) {
		res *= 2;
	}

	res = res % 5;

	std::cout << res << '\n';
}
*/